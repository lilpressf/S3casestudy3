apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: innovatech-frontend
spec:
  selector:
    app: innovatech-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort
