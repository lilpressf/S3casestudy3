apiVersion: apps/v1
kind: Deployment
metadata:
  name: innovatech-frontend
  labels:
    app: innovatech-frontend
spec:
  replicas: 1

  # IMPORTANT
  strategy:
    type: Recreate

  selector:
    matchLabels:
      app: innovatech-frontend

  template:
    metadata:
      labels:
        app: innovatech-frontend
      annotations:
        rollme: "DUMMY"

    spec:
      containers:
        - name: frontend
          image: 846244128735.dkr.ecr.eu-central-1.amazonaws.com/innovatech-frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80

          #HEALTH CHECKS
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 2
            periodSeconds: 5

          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
